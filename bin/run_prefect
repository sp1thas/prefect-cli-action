#!/usr/bin/env bash

echo "::group::Run command: prefect ${1}"

prefect_command_result=$(prefect "${1}")

exit_code=$?

echo "::set-output name=prefect-command-result::${prefect_command_result}"
exit $exit_code